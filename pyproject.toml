[project]
name = "inertia"
version = "0.1.0"
description = "A static Django webpage."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.0.5",
    "django-environ>=0.13.0",
    "django-vite>=3.1.0",
]
